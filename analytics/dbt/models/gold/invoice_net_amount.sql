select i.invoice_id,i.amount issued_amount,coalesce(sum(a.amount),0) adjusted_amount,i.amount-coalesce(sum(a.amount),0) net_amount from {{ ref('invoices') }} i left join {{ ref('invoice_adjustments') }} a using(invoice_id) group by i.invoice_id,i.amount
